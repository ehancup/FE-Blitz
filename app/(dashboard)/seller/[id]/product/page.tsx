'use client'

import { useRouter } from "next/navigation";

const Page = () => {
    const rotuer = useRouter();

    return (
        <div className="w-full h-full flex items-center justify-center">no preview yet</div>
    )
}

export default Page